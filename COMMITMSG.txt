Release v1.3: Diary Enhancements, Decor Fix, Mobile Viewport Fix

Diary:
- Sort diary entries by time descending (newest first)
- Add dynamic diary extension loading (DiaryConfig2, 3, 4...)
- Create DiaryConfig2.js with new entries (weather, mood, events, item comments)
- Change diary scrollbar color to brown for better visibility

Decor:
- Prevent unselecting active wallpaper/flooring (no more accidental re-purchase)
- Mobile: block tap on already-selected decor card

Mobile:
- Add "tap again to confirm" tooltip on previewing decor cards
- Fix iOS Safari viewport shift: lock html/body with position:fixed + 100dvh
- Add visualViewport resize listener to force scroll to top
